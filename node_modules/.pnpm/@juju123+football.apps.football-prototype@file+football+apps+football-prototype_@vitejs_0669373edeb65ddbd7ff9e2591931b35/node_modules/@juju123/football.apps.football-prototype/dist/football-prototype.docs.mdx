---
description: 'A football-prototype app.'
labels: ['app', 'react']
---

A React application for football-prototype. 

## Run the application

Import the app and add the following config to your workspace.jsonc:

```
bit use football-prototype 
```

To run the app locally, run the following:

```
bit run football-prototype 
```
