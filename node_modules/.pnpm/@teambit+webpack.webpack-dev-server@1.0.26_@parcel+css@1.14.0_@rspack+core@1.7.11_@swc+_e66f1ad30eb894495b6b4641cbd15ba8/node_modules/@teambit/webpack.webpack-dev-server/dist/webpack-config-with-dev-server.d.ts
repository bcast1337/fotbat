import type { Configuration } from 'webpack';
import type WDS from 'webpack-dev-server';
export type WebpackConfigWithDevServer = Omit<Configuration, 'devServer'> & {
    devServer: WDS.Configuration;
    favicon?: string;
};
