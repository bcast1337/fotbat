# Backend env vars — copy to .env and fill in your keys
API_FOOTBALL_KEY=your_football_data_org_key
ODDS_API_KEY=your_the_odds_api_key
PORT=3001

# Optional: persistent bet storage directory (defaults to OS temp dir)
# TRACKER_DATA_DIR=/data
